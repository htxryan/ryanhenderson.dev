---
title: "Hello, World"
pubDate: 2026-04-30
summary: "Pipeline placeholder for the blog content collection. Replace or delete during E8 migration."
tags: ["meta"]
draft: false
---

This file proves the blog content pipeline. It is a placeholder — the real content arrives in E8 (Migration & SEO Cutover).

If you are reading this on the deployed site, something has gone wrong upstream of the migration cutover.

## A quick syntax check

The post route renders code blocks via Shiki bound to the design tokens:

```ts
function greet(name: string): string {
  // Hello, world.
  return `hello, ${name}`;
}
```

<Callout variant="note" title="Placeholder">
  This entire post is a placeholder. The real content lands in E8.
</Callout>
