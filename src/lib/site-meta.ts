/**
 * Single source of truth for the site's identity strings.
 *
 * Edit `SITE_TAGLINE` here to update every visible tagline + every meta
 * description that derives from it (home hero, /about/ subtitle, both
 * routes' og:description).
 */

export const SITE_NAME = "Ryan Henderson";

/**
 * Short positioning line. Appears as the /about/ subtitle and as the
 * lead clause of the home hero tag. Composed into the `<meta>` description
 * for both routes.
 */
export const SITE_TAGLINE = "AI engineer based in Houston, Texas";
